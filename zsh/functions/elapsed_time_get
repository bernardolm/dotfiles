source_func $DOTFILES/zsh/functions/slugify

function elapsed_time_get() {
	local key=$(slugify "$@")
	local file_path="$ELAPSED_TIME_ROOT/$key"

	[ -d "$file_path" ] && return

	/bin/cat "$file_path" 2>/dev/null
}
