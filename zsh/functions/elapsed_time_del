function elapsed_time_del() {
	local key=$(slugify "$@")
	local file_path="$ELAPSED_TIME_ROOT/$key"

	local del() {
		sleep 1 && [ -f "$file_path" ] && /bin/rm -f "$file_path" >/dev/null
	}
	(del 2>&1 &)
}
