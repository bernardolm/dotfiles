
function elapsed_time_set() {
	local key=$(slugify "$@")
	local file_path="$ELAPSED_TIME_ROOT/$key"

	# NOTE: time used to calc time delta
	epoch > "$file_path"

	# ((SHELL_DEBUG)) && elapsed_time_status
}
