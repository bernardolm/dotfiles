#!/usr/bin/env zsh

if ! [ -f "${ZSH}/oh-my-zsh.sh" ]; then
    $SHELL_DEBUG && echo "installing (downloading) oh-my-zsh"
    curl -fsSL "https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh" | KEEP_ZSHRC=yes RUNZSH=no CHSH=no sh --
fi
