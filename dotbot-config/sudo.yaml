- defaults:
    link:
        force: true
        glob: true
        relink: true

- link:
    $SYNC_PATH/etc/apt/sources.list.d: /etc/apt/sources.list.d

- shell:
    - [echo "fs.file-max=131072" | sudo tee -a /etc/sysctl.conf]
    - [echo "vm.max_map_count=524288" | sudo tee -a /etc/sysctl.conf]
    - [echo "vm.swappiness=25" | sudo tee -a /etc/sysctl.conf]
    - [sudo -u $USER chsh -s $(which zsh), Making zsh the default shell]
    - [sudo dpkg --add-architecture i386]
    - [sudo ulimit -n 131072 || true]
    - [sudo ulimit -u 8192 || true]
