- defaults:
    clear:
      recursive: true
    link:
      create: true
      glob: true
    shell:
      stderr: true
      stdout: true

- link:
    ~/sync: $HOME/Dropbox/linux
    ~/shared: $HOME/Dropbox/shared

- link:
    ~/.fonts: $HOME/shared/home/.fonts
    ~/.ssh/authorized_keys: $HOME/shared/home/.ssh/authorized_keys
    ~/.ssh/config: $HOME/shared/home/.ssh/config

- link:
    ~/.bash_history: $HOME/sync/linux/home/.bash_history
    ~/.config/maestral/maestral.ini: $HOME/sync/linux/home/.config/maestral/maestral.ini
    ~/.config/rclone/rclone.conf: $HOME/sync/linux/home/.config/rclone/rclone.conf
    ~/.gitconfig: $HOME/sync/linux/home/.gitconfig
    ~/.wakatime.cfg: $HOME/sync/linux/home/.wakatime.cfg
    ~/.wget-hsts: $HOME/sync/linux/home/.wget-hsts
    ~/.zsh_history: $HOME/sync/linux/home/.zsh_history

- link:
    ~/.bash_aliases: $DOTFILES/bash/.bash_aliases
    ~/.bashrc: $DOTFILES/bash/.bashrc
    ~/.nano: $DOTFILES/git/submodules/nanorc
    ~/.zlogin: $DOTFILES/zsh/zdotdir/.zlogin
    ~/.zlogout: $DOTFILES/zsh/zdotdir/.zlogout
    ~/.zprofile: $DOTFILES/zsh/zdotdir/.zprofile
    ~/.zshenv: $DOTFILES/zsh/zdotdir/.zshenv
    ~/.zshrc: $DOTFILES/zsh/zdotdir/.zshrc
