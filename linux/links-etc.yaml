- defaults:
    clear:
      recursive: true
    link:
      glob: true
    relink: true
    shell:
      stderr: true
      stdout: true

- link:
    /etc:
      glob: true
      path: $HOME/sync/etc/apt/**
      exclude:
        - fstab.off
        - systemd
        - wsl.conf.off
