- defaults:
    # clear:
    #   recursive: true
    link:
      create: true
      # force: true
      # ignore-missing: true
    # relink: true
    shell:
      stderr: true
      stdout: true

- link:
    /etc:
      glob: true
      path: $HOME/sync/etc/**
