[user]
    name = Bernardo Loureiro

[github]
    user = bernardolm

[color]
    branch = true
    diff = true
    status = true
    ui = true

[push]
    default = matching
    followTags = true

[pull]
    default = matching

[url "git@github.com:"]
    insteadOf = "gh:"
    insteadOf = "https://github.com"
    pushInsteadOf = "git://github.com"
    pushInsteadOf = "github:"

[core]
    editor = code --wait
    untrackedCache = true
    whitespace = space-before-tab,-indent-with-non-tab,trailing-space

[gpg]
    format = ssh
    program = gpg

[alias]
    mv-index = "!f() { \
        old=\"$1\"; \
        new=\"$2\"; \
        info=$(git ls-files -s -- \"$old\" | cut -d' ' -f-2 | tr ' ' ,); \
        git update-index --add --cacheinfo \"$info,$new\" && \
        git rm --cached \"$old\"; \
    }; f"

[merge]
#    external = $DOTFILES/zsh/git-diff.sh
    log = true

[apply]
    whitespace = fix

[difftool]
    prompt = false

[help]
    autocorrect = 1

[diff]
#    external = $DOTFILES/zsh/git-diff.sh
    tool = default-difftool
    renames = copies

[difftool "default-difftool"]
    cmd = code --wait --diff $LOCAL $REMOTE

[init]
    defaultBranch = main

[commit]
    gpgsign = true

[branch]
    sort = -committerdate

[include]
    path = ~/Sync/config-backup/.gitconfig_work
[safe]
	directory = /home/bernardo/workspaces/bernardolm/dotfiles/dotbot
	directory = /home/bernardo/workspaces/bernardolm/dotfiles/dotbot
